django>=5.1,<6
django-ninja>=1.3
psycopg[binary]>=3.2
python-dotenv>=1.0
django-cors-headers>=4.4
httpx>=0.27
openpyxl>=3.1.5
gunicorn>=23.0
whitenoise>=6.7
django-q2>=1.7
# Uploaded evidence lives in Google Cloud Storage — a Cloud Run container
# filesystem is discarded with the instance.
django-storages[google]>=1.14

# Reports leave this system in five shapes. Excel and CSV are for people who
# will do more arithmetic; PDF is for the ones who will sign it, and DOCX for
# the ones who will paste it into a submission with a covering paragraph.
reportlab>=4.2
python-docx>=1.1

# Sign-in providers. Both verify a token against the issuer's published keys
# rather than holding a secret, so neither needs a credential in settings.
#
# google-auth was already imported by /auth/google and never declared here --
# it arrives transitively through django-storages[google], which means the
# Google sign-in worked by luck and would have broken the first time that
# dependency was dropped or pinned differently.
google-auth>=2.35
PyJWT[crypto]>=2.9
requests>=2.32

# Property-based testing for the money logic (core/test_logic.py). Test-only;
# the runtime never imports it. Example budgets there are bounded and
# derandomised, so a payroll property gives the same answer on every run.
hypothesis>=6.100

# Django's --parallel test runner needs tblib to reassemble tracebacks from
# worker subprocesses. Test-only; the runtime never imports it.
tblib>=3.0
